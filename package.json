{
  "name": "hystersis-ascii",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --port 8765 --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --port 8765",
    "deploy": "npm run build && wrangler deploy",
    "test": "node --test src/*.test.mjs"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.3",
    "vite": "^6.2.0",
    "wrangler": "^4.0.0"
  }
}
